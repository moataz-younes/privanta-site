/** Logo served from `public/logoo.png`. Replace that file with a true-transparent PNG (alpha) — a white matte in the file will always show as a white box. */
export const BRAND_LOGO_SRC = "/logoo.png";
